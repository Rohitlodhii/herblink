# Copy to .env and set your values
DATABASE_URL=postgres://username:password@localhost:5432/admin_db
# Example for Windows PowerShell use (replace values):
# $env:DATABASE_URL='postgres://user:pass@localhost:5432/admin_db'
# Admin credentials for simple login (change in production)
ADMIN_USER=admin
ADMIN_PASS=password

# Secret for signing JWTs — set a strong random value
JWT_SECRET=replace_with_strong_secret

# Set these values in your environment before running the server.
# Example (PowerShell):
# $env:ADMIN_USER='myadmin'
# $env:ADMIN_PASS='supersecret'
# $env:JWT_SECRET='very-long-random-secret'

# When using external admin APIs (no backend), set the public base URL used by the frontend.
# This should include protocol, e.g. https://api.example.com
# Example (PowerShell):
# $env:NEXT_PUBLIC_API_BASE_URL = 'https://api.example.com'
