import { AuthenticatedRequest } from "../../middlewares/jwt.verify.js";


async function addHerb( req  :AuthenticatedRequest , res  : Response) {
    try {
        const userId = req.userId;
        
    } catch (error) {
        
    }
}